// Handles all the crap relating to custom chess games.
/*
bool ChessMap = 0;

void ChessSpawn( edict_t *pent )
{
	// ent spawn... really just to check if this map IS a chess map.

	char *pClassname = (char *)STRING(pent->v.classname);
	

	if (FStrEq(pClassname, "info_chess");
	{
		ChessMap = 1;
	}
}

void ChessTouch( edict_t *pent, edict_t *pentOther )
{

	// test mode... light up the ent.

	char *pClassname = (char *)STRING(pent->v.classname);




}
*/